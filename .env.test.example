# An example .env.test to document which variables are needed for testing with placeholder values. Copy this to .env.test or export the variable in your shell before running integration tests.

DATABASE_URL_TEST=postgres://postgres:postgres@localhost:5432/web_scraper_test